# Data Notes

The files in `data/pilot/` are tiny sanity-check datasets. They are useful for
testing the evaluation pipeline, output format, and analysis scripts.

Do not treat these tiny files as final paper evidence.

For the workshop paper, replace or supplement them with standard benchmark
subsets through `lm-evaluation-harness`, plus a clearly documented
code-switched set.

