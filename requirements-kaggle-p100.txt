# Kaggle P100 has CUDA compute capability sm_60. Avoid PyTorch's default CUDA
# 13 wheel, which may not include sm_60 kernels. Install PyTorch from the CUDA
# 12.6 wheel index first, then install the remaining packages.
--index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://pypi.org/simple

torch
torchvision
torchaudio

accelerate>=0.34.0
bitsandbytes==0.49.2
datasets>=2.20.0
huggingface-hub>=0.24.0
lm-eval>=0.4.4
matplotlib>=3.8.0
numpy>=1.26.0
pandas>=2.2.0
pyyaml>=6.0.1
scipy>=1.12.0
seaborn>=0.13.0
transformers>=4.44.0,<5
